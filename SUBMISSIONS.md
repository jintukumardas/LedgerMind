# Submission checklist

One section per track. Each stated qualification requirement maps to the exact
file, command, or artifact that satisfies it, so it can be verified line by
line.

Baseline for all continuity claims: tag
[`pre-ethonline-2026`](https://github.com/jintukumardas/LedgerMind/tree/pre-ethonline-2026)
= commit `9b9a304`. Everything after it was built during the event.

**Fill before submitting:** items marked ⬜ need a value only you can supply.

---

## 1. The Graph — Best AI Tooling or AI Use Case (Continuity pool)

| # | Requirement | Satisfied by | Status |
|---|---|---|---|
| 1 | Use The Graph as a load-bearing component | [`packages/subgraph`](packages/subgraph) is the only data path for `/api/v1/*` and the assistant. [`apps/web/src/lib/subgraph.ts`](apps/web/src/lib/subgraph.ts) throws if `SUBGRAPH_URL` is unset — there is no RPC or Postgres fallback. | ✅ |
| 2 | Consume live data from a Graph provider (not mocked/local) | Deployed to Subgraph Studio on `sei-atlantic`, synced with no indexing errors:<br>`https://api.studio.thegraph.com/query/1760259/ledgermind/v0.0.2`<br>Studio: <https://thegraph.com/studio/subgraph/ledgermind>. Every API response echoes `source.indexedBlock`. | ✅ |
| 3 | Meaningful work: reasoning, decisions, automation, or NL interface | [`/api/v1/analyze`](apps/web/src/app/api/v1/analyze/route.ts) returns ranked `findings` and typed `actions` (`revoke_intent`, `withdraw_remainder`, `reduce_total_cap`, `restrict_merchants`, …) each with `recoversUsdc`, `urgency` and `evidence`. It does not print query results. | ✅ |
| 4 | Open source with clear README | [`README.md`](README.md), [`packages/subgraph/README.md`](packages/subgraph/README.md). MIT licensed. | ✅ |
| 5 | Public repository | <https://github.com/jintukumardas/LedgerMind> | ✅ |
| 6 | 2–4 minute demo video | [`docs/demo-script.md`](docs/demo-script.md) §1 | ⬜ record |
| 7 | Select the **Continuity** pool | On the ETHGlobal submission form | ⬜ select |

### Verification commands

```bash
# The subgraph has live data
curl -s -X POST "$SUBGRAPH_URL" -H 'Content-Type: application/json' \
  -d '{"query":"{ _meta { block { number } hasIndexingErrors } protocol(id:\"0x676c6f62616c\") { intentCount paymentCount totalVolume } }"}' | jq

# The API is backed by it
curl -s localhost:3000/api/v1/intents | jq '.source'

# The assistant reasons rather than dumps
curl -s -X POST localhost:3000/api/v1/analyze \
  -H 'Content-Type: application/json' \
  -d '{"payer":"0x0bD89b308B2497e79b48cF52163fDc98aC5663c2"}' \
  | jq '.data.actions'
```

### Why the Postgres indexer was replaced

Seven defects with line references, verified against the committed code at the
baseline tag, in
[`packages/subgraph/README.md`](packages/subgraph/README.md). Summary: a hard
1000-block lookback window that silently loses data after ~6.7 minutes of
downtime, no reorg handling, no backfill, and O(N) RPC fan-out per tick.

The Postgres path is still in the tree
([`packages/mcp/src/indexer.ts`](packages/mcp/src/indexer.ts),
[`packages/mcp/src/database/`](packages/mcp/src/database)) and is deletable —
nothing in the judged path imports it.

### Live data created during the event

Seeded by [`scripts/seed-demo-intents.mjs`](scripts/seed-demo-intents.mjs),
payer `0x0bD89b308B2497e79b48cF52163fDc98aC5663c2`, first intent at block
`270805825`:

| Intent | Address | Shape it exercises |
|---|---|---|
| throttled | `0x4E0866D3C7Ac1ACBD4058Cc9a0877193E640bcf4` | payments at 98% of perTxCap |
| over-authorised | `0xb530E9CA418560028Db414E09Aa36617A89E7cA7` | 50 USDC cap, 0.1 USDC spent |
| unrestricted | `0x568222982112B7BD55Bd90524E1434472F422f5b` | empty merchant allowlist |
| revoked-idle | `0xE88B02aBD36A10617386cD71105FF39276ccdf16` | 0.8 USDC recoverable |

Record: [`docs/demo-intents.json`](docs/demo-intents.json).

---

## 2. Bazantic — Help an Agent Use Your Hackathon Project

| # | Requirement | Satisfied by | Status |
|---|---|---|---|
| 1 | Create an account on bazantic.com | — | ⬜ create |
| 2 | Create an x402/MPP Gateway for the project | OpenAPI 3.1 document served at `/api/v1/openapi`, generated by [`lib/openapi.ts`](apps/web/src/lib/openapi.ts) | ⬜ register |
| 3 | Build a Recipe explaining service usage | [`docs/bazantic/recipe.md`](docs/bazantic/recipe.md) — when to reach for it, how to read units, the six mistakes that produce wrong answers, a worked example | ✅ drafted |
| 4 | Identical prompt/model/settings in comparative tests | [`scripts/bazantic-ab.mjs`](scripts/bazantic-ab.mjs) — both arms read the same `TASKS`, `TOOLS`, `MODEL`, `MAX_TOKENS`, `MAX_TURNS` constants; only `ARMS.B_with_recipe` appends the Recipe | ✅ |
| 5 | Recipe is the only material difference | `ARMS` in that script is literally `{A: BASE_SYSTEM, B: BASE_SYSTEM + RECIPE}`. `inputs.json` in each run directory records both prompts verbatim. | ✅ |
| 6 | Logged inputs and outputs | `docs/bazantic-ab/run-<timestamp>/` — per-arm transcripts, `inputs.json`, `SUMMARY.md` | ⬜ run |
| 7 | Video documenting outcome improvements | [`docs/demo-script.md`](docs/demo-script.md) §2 | ⬜ record |
| 8 | Provide Bazantic account username | — | ⬜ supply |

### Running the A/B test

```bash
export ANTHROPIC_API_KEY=...
export LEDGERMIND_API=https://<your-deployment>
export PAYER=0x0bD89b308B2497e79b48cF52163fDc98aC5663c2
node scripts/bazantic-ab.mjs
```

The harness probes the API first and exits non-zero if it is not live, so a run
directory can never contain fixture-derived results.

### The API surface

| Endpoint | Purpose |
|---|---|
| `GET /api/v1/intents` | list spending authorities, filter by payer/agent/state |
| `GET /api/v1/intents/{address}` | one authority with payment history |
| `GET /api/v1/payments` | executed payments, filter by intent/agent/merchant/time |
| `POST /api/v1/analyze` | findings and recommended actions |
| `GET /api/v1/openapi` | the OpenAPI 3.1 document |

---

## 3. Ledger — Continuity

**Not submitted.** The track requires either a hardware signer, Key Ring
secrets, or a device confirmation. Per
[the Key Ring docs](https://developers.ledger.com/docs/ai-tools/ledger-cli),
`ring init` requires physical device interaction, and no Ledger device was
available during the event. Attempting it would have produced an unrunnable
demo.

Recorded here rather than omitted, because the flaw it would have fixed is
real and is documented in [`AI_USAGE.md`](AI_USAGE.md): `PRIVATE_KEY_PAYER` and
`PRIVATE_KEY_AGENT` are still plain env vars.

---

## Security

| Finding | Detail | Action |
|---|---|---|
| Live private key in `README.md` | 64-hex literal used for both `PRIVATE_KEY_PAYER` and `PRIVATE_KEY_AGENT` in a `claude mcp add` example. Introduced in `9795ee6` (2025-08-24), public since. Address `0x0bD89b308B2497e79b48cF52163fDc98aC5663c2` — 9.97 SEI, 5 USDC, nonce 15 on Sei testnet; zero balance and nonce 0 on mainnet. | Replaced with a placeholder. Wallet treated as permanently compromised and used only for testnet demo data. |
| `NEXT_PUBLIC_AGENT_PRIVATE_KEY` | The `NEXT_PUBLIC_` prefix inlines the value into the browser bundle, readable by any visitor. Still read by [`apps/web/src/lib/agent-wallet.ts`](apps/web/src/lib/agent-wallet.ts), imported only by `agent-demo.tsx`. | **Not fixed.** Removed from `.env.example` with an explanation; left unset, `agent-wallet.ts` falls back to a throwaway random key and warns. No judged code path (`/api/v1/*`, the subgraph client, the assistant) touches it. A real fix means moving agent signing server-side, which was out of scope for the time available. |
| Full-history secret scan | `git log -p --all` across `*.md,*.ts,*.tsx,*.js,*.json,*.sh,*.sol,*.yml,*.toml,*.env*` for secret-shaped assignments and JWTs | No other real secrets. Every other match is a `your_*_here` placeholder. No `.env` was ever committed — only `.env.example` files. |

Git history was **not** rewritten. The key is already public and burned;
rewriting would move the `pre-ethonline-2026` baseline tag that the continuity
claim depends on, for no security gain.
