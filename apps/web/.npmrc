# The dependency tree has a pre-existing peer conflict: @reown/appkit-wallet
# pins zod@3.22.4 while other packages want a newer zod. Strict resolution
# fails, including on Vercel. This keeps local and CI installs identical.
legacy-peer-deps=true
